#include <iostream>
#include <vector>
using namespace std;
class Account
{
private:
    int ID_customer;
    int ID_account;
    int statedDate;
    double balance;

public:
    Account(/* args */);
   
};

class Trade
{
private:
   int state;
public:
    Traced(/* args */);
    ~Traced();
};

class Bank
{
private:
    int public : Bank(/* args */);
    ~Bank();
};

Bank::Bank(/* args */)
{
}

Bank::~Bank()
{
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    return 0;
}