#include <iostream>
#include <vector>
using namespace std;

int main()
{

    int adj[100][100];
    int n, m;
    cin >> n >> m;
    ttt
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    return 0;
}